app_name: sendpulse
server:
  address: ":8080"
  mode: dev
database:
  dsn: "postgres://postgres:postgres@localhost:5432/sendpulse?sslmode=disable"
messaging:
  interval: 5s
  batch_size: 10
  max_retries: 2
  retry_delay: 2s
  enabled: true
webhook:
  url: "https://webhook.site/9925f1cc-04f9-4daa-a2ad-e2cb7f4ba2ee"
